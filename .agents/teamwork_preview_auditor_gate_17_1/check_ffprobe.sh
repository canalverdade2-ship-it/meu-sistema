which ffprobe || true
type ffprobe || true
if [ -f /usr/local/bin/ffprobe ]; then cat /usr/local/bin/ffprobe; fi
if [ -f /usr/bin/ffprobe ]; then file /usr/bin/ffprobe; fi
docker images | grep control-plane
