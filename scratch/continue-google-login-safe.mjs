import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer'),fs=require('fs'),cp=require('child_process'),crypto=require('crypto');const sleep=ms=>new Promise(r=>setTimeout(r,ms));function unlock(){const v=JSON.parse(fs.readFileSync('/home/opc/gsa-ai/secrets/google-production.enc.json'));const key=Buffer.from(cp.execFileSync('sudo',['docker','exec','gsa-tv-control-plane','printenv','GSA_TV_SECRET_KEY'],{encoding:'utf8'}).trim(),'hex'),n=Buffer.from(v.nonce,'base64url'),a=Buffer.from(v.ciphertext,'base64url'),d=crypto.createDecipheriv('aes-256-gcm',key,n);d.setAAD(Buffer.from(v.aad));d.setAuthTag(a.subarray(-16));return JSON.parse(Buffer.concat([d.update(a.subarray(0,-16)),d.final()]));}
(async()=>{const c=unlock(),b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});for(const p of (await b.pages()).filter(x=>x.url().includes('accounts.google'))){const kind=p.url().includes('service=wise')?'VIDS':'FLOW';if(await p.$('input[type=password]')){const input=await p.$('input[type=password]');const has=await input.evaluate(e=>e.value.length>0);if(!has)await input.type(c.password,{delay:10});const btn=await p.evaluateHandle(()=>[...document.querySelectorAll('button')].find(e=>(e.innerText||'').trim()==='Next'));if(btn.asElement())await btn.asElement().click();}
else {const el=await p.$('[data-email="'+c.email+'"]');if(el)await el.click();}
await sleep(7000);console.log(kind,p.url().includes('accounts.google')?'AUTH_PENDING':'AUTH_OK',(await p.evaluate(()=>document.body.innerText)).slice(0,400).replace(/\s+/g,' '));}await b.disconnect()})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/continue-login.js\ntimeout 70s node /tmp/continue-login.js\nrm -f /tmp/continue-login.js`,90000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
