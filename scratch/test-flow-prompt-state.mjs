import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),p=(await b.pages()).find(x=>x.url().includes('flow.google.com/project/'));console.log(await p.evaluate(()=>{const i=document.querySelector('input[aria-label="Texto editável"]'),g=document.querySelector('button[aria-label="Iniciar geração"]');return {value:i?.value||'',disabled:g?.disabled,aria:g?.getAttribute('aria-disabled'),html:i?.outerHTML.slice(0,500)}}));await b.disconnect()})()`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/test-flow.js\nnode /tmp/test-flow.js\nrm -f /tmp/test-flow.js`,20000);process.stdout.write(r.stdout);
