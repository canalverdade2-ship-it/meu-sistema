import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),p=(await b.pages()).find(x=>x.url().includes('flow.google.com/project/')),i=await p.$('input[aria-label="Texto editável"]');await i.focus();await p.keyboard.down('Control');await p.keyboard.press('A');await p.keyboard.up('Control');await p.keyboard.press('Backspace');await p.keyboard.type('Animated weather map, moving clouds, gentle rain, blue and gold, no people, no words, landscape.',{delay:0});console.log('TYPED');process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/type-flow-only.js\ntimeout 12s node /tmp/type-flow-only.js || true\nrm -f /tmp/type-flow-only.js`,20000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
