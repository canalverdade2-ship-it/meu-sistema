import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});for(const p of (await b.pages()).filter(x=>x.url().includes('accounts.google'))){console.log('===PAGE===',p.url().includes('service=wise')?'VIDS':'FLOW',p.url());console.log((await p.evaluate(()=>document.body.innerText)).slice(0,2000));console.log(JSON.stringify(await p.evaluate(()=>[...document.querySelectorAll('input,button,[role=button],[role=link],[data-email]')].filter(e=>e.getBoundingClientRect().width>0).slice(0,60).map(e=>({tag:e.tagName,type:e.type||'',text:(e.innerText||e.value||'').slice(0,80),aria:e.getAttribute('aria-label')||'',email:e.getAttribute('data-email')||'',id:e.id||''}))),null,2));}await b.disconnect()})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/inspect-login.js\nnode /tmp/inspect-login.js\nrm -f /tmp/inspect-login.js`,60000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
