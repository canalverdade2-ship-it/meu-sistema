import {runSshScript} from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),p=(await b.pages()).find(x=>x.url().includes('/videos/d/')),sleep=m=>new Promise(r=>setTimeout(r,m));for(let i=0;i<5;i++){const pos=await p.evaluate(()=>{const e=[...document.querySelectorAll('[role=button]')].find(x=>(x.getAttribute('aria-label')||'').startsWith('Nova cena'));if(!e)return null;const r=e.getBoundingClientRect();return{x:r.x+r.width/2,y:r.y+r.height/2}});if(!pos)throw new Error('Nova cena não encontrada');await p.mouse.click(pos.x,pos.y);await sleep(500);}const scenes=await p.evaluate(()=>[...document.querySelectorAll('[role=button][aria-label^="Cena "]')].map(x=>x.getAttribute('aria-label')));console.log(JSON.stringify({scene_count:scenes.length,scenes}));await b.disconnect()})().catch(e=>{console.error(e);process.exit(1)});`;
const j64=Buffer.from(js).toString('base64');const r=await runSshScript(String.raw`printf '%s' '${j64}'|base64 -d >/tmp/vids-add-scenes.js
node /tmp/vids-add-scenes.js
rm -f /tmp/vids-add-scenes.js`,60000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
