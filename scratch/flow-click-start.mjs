import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),p=(await b.pages()).find(x=>x.url().includes('flow.google.com/project/'));const h=await p.evaluateHandle(()=>[...document.querySelectorAll('button')].find(e=>(e.innerText||'').trim()==='Começar'));if(h.asElement()){const r=await h.asElement().boundingBox();await p.mouse.click(r.x+r.width/2,r.y+r.height/2);console.log('CLICKED_START')}else console.log('NO_START');await b.disconnect()})()`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/flow-start.js\nnode /tmp/flow-start.js\nrm -f /tmp/flow-start.js`,20000);process.stdout.write(r.stdout);
