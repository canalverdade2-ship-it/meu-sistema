import {runSshScript} from './ssh2-run.mjs';const remote=String.raw`sudo docker run --rm -v /opt/gsa-tv/cache/media:/media:ro gsa-tv/control-plane:1.6.13 sh -lc 'total=0; for f in /media/1/news/gsa-news-2026-09-01-v2/audio/*.wav; do d=$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 "$f"); printf "%s|%.2f\n" "$(basename "$f")" "$d"; total=$(awk -v a="$total" -v b="$d" "BEGIN{print a+b}"); done; printf "TOTAL_SECONDS|%.2f\n" "$total"'`;const r=await runSshScript(remote,30000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
