import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});for(const p of await b.pages()){const k=p.url().includes('flow.google.com/project/')?'FLOW':p.url().includes('/videos/d/')?'VIDS':null;if(!k)continue;console.log('===',k,'===');console.log((await p.evaluate(()=>document.body.innerText)).slice(-5000));console.log('VIDEOS',await p.evaluate(()=>[...document.querySelectorAll('video')].map(v=>({src:!!v.src,ready:v.readyState,duration:v.duration,paused:v.paused}))));}await b.disconnect()})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/check-gen.js\nnode /tmp/check-gen.js\nrm -f /tmp/check-gen.js`,30000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
