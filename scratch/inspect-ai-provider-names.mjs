import {runSshScript} from './ssh2-run.mjs';const remote=String.raw`set -e; dburl=$(sudo docker inspect -f '{{range .Config.Env}}{{println .}}{{end}}' gsa-tv-control-plane | sed -n 's/^DATABASE_URL=//p'); sudo docker run --rm --network host postgres:15-alpine psql "$dburl" -X -At -F '|' -c "select column_name from information_schema.columns where table_schema='public' and table_name='gsa_tv_ai_provider_secrets' order by ordinal_position; select provider from public.gsa_tv_ai_provider_secrets order by provider;"`;const r=await runSshScript(remote,30000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
