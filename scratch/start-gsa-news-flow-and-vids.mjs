import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});let pages=await b.pages();let f=pages.find(p=>p.url().includes('flow.google.com/project/'));const prompt='8-second photorealistic premium Brazilian television news support clip for GSA News: severe weather system over Southern and Southeastern Brazil, dark storm clouds, rain over a modern city and meteorological radar visualization, slow cinematic camera movement, factual public-service broadcast style, no destruction, no people in danger, no readable text, no logo, no watermark, horizontal 16:9.';const inp=await f.$('input[aria-label="Texto editável"]');await inp.click();await inp.type(prompt,{delay:1});const go=await f.$('button[aria-label="Iniciar geração"]');await go.click();console.log('FLOW_SUBMITTED');let v=pages.find(p=>p.url().includes('/videos/d/'));const title=await v.$('input[aria-label="Renomear"]');await title.click({clickCount:3});await title.type('GSA News - 04-09-2026',{delay:5});await v.keyboard.press('Enter');const ai=await v.evaluateHandle(()=>[...document.querySelectorAll('button')].find(e=>(e.innerText||'').includes('Criar vídeos com IA')));if(!ai.asElement())throw new Error('botão Vids IA ausente');await ai.asElement().evaluate(e=>e.click());console.log('VIDS_AI_CLICKED');await b.disconnect()})().catch(e=>{console.error(e.stack||e);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/start-news.js\nnode /tmp/start-news.js\nrm -f /tmp/start-news.js`,30000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
