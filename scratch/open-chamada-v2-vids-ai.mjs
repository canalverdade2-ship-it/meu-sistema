import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');const sleep=ms=>new Promise(r=>setTimeout(r,ms));(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});const p=(await b.pages()).find(x=>x.url().includes('/videos/d/1NoUuv9KmksylGMfxKdbcXCvXHYZ9k89XvcwAhwKErjo/'));const h=await p.evaluateHandle(()=>[...document.querySelectorAll('button')].find(e=>(e.innerText||'').includes('Criar vídeos com IA')));if(!h.asElement())throw new Error('Botão IA ausente');await h.asElement().click();await sleep(2500);console.log((await p.evaluate(()=>document.body.innerText)).slice(0,7000));console.log(JSON.stringify(await p.evaluate(()=>[...document.querySelectorAll('button,input,textarea,[contenteditable=true]')].filter(e=>{const r=e.getBoundingClientRect();return r.width>0&&r.height>0}).slice(0,160).map((e,i)=>({i,tag:e.tagName,aria:e.getAttribute('aria-label')||'',ph:e.getAttribute('placeholder')||'',text:(e.innerText||e.textContent||e.value||'').trim().slice(0,160)}))),null,2));await b.disconnect()})().catch(e=>{console.error(e.stack||e);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64'); const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/o.js\nnode /tmp/o.js\nrm -f /tmp/o.js`,60000);process.stdout.write(r.stdout);
