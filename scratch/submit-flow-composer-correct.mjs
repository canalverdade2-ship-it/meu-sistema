import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),p=(await b.pages()).find(x=>x.url().includes('flow.google.com/project/')),e=await p.$('.ProseMirror[contenteditable=true]');await e.click();await p.keyboard.type('Animated regional weather map with moving clouds and gentle rainfall over a generic coastal city. Calm blue and gold public information style. Smooth camera movement. No people, no damage, no words, no logos, landscape format.',{delay:0});const g=await p.$('button[aria-label="Iniciar geração"]');console.log('DISABLED',await g.evaluate(x=>x.disabled));if(!(await g.evaluate(x=>x.disabled)))await g.click();console.log('SUBMITTED');process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/flow-correct.js\ntimeout 15s node /tmp/flow-correct.js || true\nrm -f /tmp/flow-correct.js`,20000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
