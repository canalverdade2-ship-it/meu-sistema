import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});for(const p of await b.pages()){let k=p.url().includes('flow.google.com')?'FLOW':p.url().includes('/videos/d/')?'VIDS':null;if(!k)continue;console.log('===',k,'===',p.url());console.log((await p.evaluate(()=>document.body.innerText)).slice(0,6000));console.log('FIELDS',JSON.stringify(await p.evaluate(()=>[...document.querySelectorAll('textarea,input,[contenteditable=true],button')].filter(e=>e.getBoundingClientRect().width>0).slice(0,120).map((e,i)=>({i,tag:e.tagName,aria:e.getAttribute('aria-label')||'',ph:e.getAttribute('placeholder')||'',text:(e.innerText||'').trim().slice(0,100)}))),null,2));}await b.disconnect()})().catch(e=>{console.error(e.stack||e);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/inspect-prod.js\nnode /tmp/inspect-prod.js\nrm -f /tmp/inspect-prod.js`,60000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
