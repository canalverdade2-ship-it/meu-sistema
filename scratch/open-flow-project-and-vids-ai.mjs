import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');const sleep=ms=>new Promise(r=>setTimeout(r,ms));(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});let p=(await b.pages()).find(x=>x.url().includes('flow.google.com'));let h=await p.evaluateHandle(()=>[...document.querySelectorAll('button')].find(e=>(e.innerText||'').includes('Novo projeto')));if(h.asElement()){await h.asElement().click();await sleep(4000);}console.log('FLOW',p.url(),(await p.evaluate(()=>document.body.innerText)).slice(-3500));p=(await b.pages()).find(x=>x.url().includes('/videos/d/'));h=await p.evaluateHandle(()=>[...document.querySelectorAll('button')].find(e=>(e.innerText||'').includes('Criar vídeos com IA')));if(h.asElement()){await h.asElement().click();await sleep(2500);}console.log('VIDS',p.url(),(await p.evaluate(()=>document.body.innerText)).slice(0,5000));await b.disconnect()})().catch(e=>{console.error(e.stack||e);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/open-prod.js\nnode /tmp/open-prod.js\nrm -f /tmp/open-prod.js`,60000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
