import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer'),fs=require('fs'),cp=require('child_process'),crypto=require('crypto');function unlock(){const v=JSON.parse(fs.readFileSync('/home/opc/gsa-ai/secrets/google-production.enc.json'));const k=Buffer.from(cp.execFileSync('sudo',['docker','exec','gsa-tv-control-plane','printenv','GSA_TV_SECRET_KEY'],{encoding:'utf8'}).trim(),'hex'),n=Buffer.from(v.nonce,'base64url'),a=Buffer.from(v.ciphertext,'base64url'),d=crypto.createDecipheriv('aes-256-gcm',k,n);d.setAAD(Buffer.from(v.aad));d.setAuthTag(a.subarray(-16));return JSON.parse(Buffer.concat([d.update(a.subarray(0,-16)),d.final()]));}(async()=>{const c=unlock(),b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),pages=(await b.pages()).filter(p=>p.url().includes('/signin/challenge/pwd'));console.log('PAGES',pages.length);for(const p of pages){await p.evaluate(password=>{const i=document.querySelector('input[type=password]');const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set;setter.call(i,password);i.dispatchEvent(new Event('input',{bubbles:true}));i.dispatchEvent(new Event('change',{bubbles:true}));document.querySelector('#passwordNext button,#passwordNext')?.click();},c.password);console.log('SUBMITTED');break;}await b.disconnect()})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/submit-one.js\nnode /tmp/submit-one.js\nrm -f /tmp/submit-one.js`,30000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
