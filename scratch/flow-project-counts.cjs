const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');
(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});for(const p of await b.pages())if(p.url().includes('flow.google.com/project/'))console.log(p.url(),await p.evaluate(()=>({tiles:document.querySelectorAll('flow-grid-tile-container').length,videos:document.querySelectorAll('flow-video-tile').length,thumb:document.querySelectorAll('flow-video-tile img.thumbnail').length,imgs:[...document.querySelectorAll('flow-video-tile img')].map(x=>({c:x.className,src:x.src.slice(0,100)}))})));await b.disconnect()})().catch(e=>{console.error(e);process.exit(1)});
