import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'});const p=(await b.pages()).find(x=>x.url().includes('/videos/d/1NoUuv9KmksylGMfxKdbcXCvXHYZ9k89XvcwAhwKErjo/'));if(!p)throw new Error('V2 ausente');console.log('TEXT', (await p.evaluate(()=>document.body.innerText)).slice(0,6000));console.log('FIELDS',JSON.stringify(await p.evaluate(()=>[...document.querySelectorAll('button,input,textarea,[contenteditable=true]')].filter(e=>{const r=e.getBoundingClientRect();return r.width>0&&r.height>0}).slice(0,200).map((e,i)=>({i,tag:e.tagName,aria:e.getAttribute('aria-label')||'',ph:e.getAttribute('placeholder')||'',text:(e.innerText||e.textContent||e.value||'').trim().slice(0,130)}))),null,2));await b.disconnect()})().catch(e=>{console.error(e.stack||e);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64'); const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/i.js\nnode /tmp/i.js\nrm -f /tmp/i.js`,60000);process.stdout.write(r.stdout);
