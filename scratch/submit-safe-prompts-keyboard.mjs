import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),ps=await b.pages();let v=ps.find(p=>p.url().includes('/videos/d/')),box=await v.$('[aria-label^="Descreva o vídeo"]');await box.click();await v.keyboard.down('Control');await v.keyboard.press('A');await v.keyboard.up('Control');await v.keyboard.type('Elegant animated studio background. Navy blue and warm gold colors. Abstract map screens. Empty modern desk. Smooth camera movement. Clean lighting. No people. No words. Landscape format.',{delay:0});console.log('VIDS_TYPED');await (await v.$('button[aria-label="Gerar"]')).click();console.log('VIDS_GENERATE_CLICKED');let f=ps.find(p=>p.url().includes('flow.google.com/project/')),inp=await f.$('input[aria-label="Texto editável"]');await inp.click();await f.keyboard.type('Animated regional weather map with moving clouds and gentle rainfall over a generic coastal city. Calm blue and gold public information style. Smooth camera movement. No people, no damage, no words, landscape format.',{delay:0});console.log('FLOW_TYPED');await (await f.$('button[aria-label="Iniciar geração"]')).click();console.log('FLOW_GENERATE_CLICKED');await b.disconnect()})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/submit-safe-key.js\ntimeout 20s node /tmp/submit-safe-key.js\nrm -f /tmp/submit-safe-key.js`,30000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
