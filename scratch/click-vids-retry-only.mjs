import { runSshScript } from './ssh2-run.mjs';
const js=String.raw`const puppeteer=require('/usr/lib/node_modules/@wonderwhy-er/desktop-commander/node_modules/puppeteer');(async()=>{const b=await puppeteer.connect({browserURL:'http://127.0.0.1:9228'}),p=(await b.pages()).find(x=>x.url().includes('/videos/d/'));const h=await p.evaluateHandle(()=>[...document.querySelectorAll('*')].find(e=>(e.innerText||'').trim()==='Tentar de novo'&&e.getBoundingClientRect().width>0));const e=h.asElement();if(!e)throw new Error('retry ausente');const r=await e.boundingBox();console.log(JSON.stringify(r));await p.mouse.click(r.x+r.width/2,r.y+r.height/2);console.log('CLICKED');await b.disconnect()})().catch(e=>{console.error(e.message);process.exit(1)});`;
const enc=Buffer.from(js).toString('base64');const r=await runSshScript(`printf '%s' '${enc}'|base64 -d >/tmp/click-retry.js\nnode /tmp/click-retry.js\nrm -f /tmp/click-retry.js`,20000);process.stdout.write(r.stdout);if(r.stderr)process.stderr.write(r.stderr);
