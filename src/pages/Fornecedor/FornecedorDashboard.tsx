import { useCallback, useEffect, useMemo, useState } from 'react';
import {
  Bell, Building2, FileCheck2, FileUp, LayoutDashboard, LogOut, Menu,
  Package, Plus, RefreshCw, ShoppingCart, Truck, WalletCards, X,
} from 'lucide-react';
import { toast } from 'react-hot-toast';
import { Modal } from '../../components/ui/Modal';
import { LogoGSA } from '../../components/ui/LogoGSA';
import { formatCurrency, formatDate, formatDateTime, generateUUID } from '../../lib/utils';
import {
  getSupplierSnapshot, markSupplierOrderSeen, requestSupplierProduct,
  submitSupplierDelivery, uploadSupplierInvoice,
} from '../../lib/supplierOperations';
import { navigate } from '../../routing/navigationService';
import { routes } from '../../routing/routeCatalog';
import { useAppLocation } from '../../routing/useAppLocation';
import type { SupplierOrder, SupplierSnapshot } from '../../types/supplier';

const EMPTY: SupplierSnapshot = { supplier: {}, products: [], catalog: [], requests: [], orders: [], deliveries: [], payables: [], notifications: [] };

const MENU = [
  { id: 'dashboard', label: 'Dashboard', icon: LayoutDashboard, path: routes.supplier.dashboard() },
  { id: 'produtos', label: 'Produtos', icon: Package, path: routes.supplier.products() },
  { id: 'pedidos', label: 'Pedidos de compra', icon: ShoppingCart, path: routes.supplier.orders() },
  { id: 'entregas', label: 'Entregas e NFs', icon: Truck, path: routes.supplier.deliveries() },
  { id: 'financeiro', label: 'Financeiro', icon: WalletCards, path: routes.supplier.payables() },
];

export function FornecedorDashboard({ fornecedorId, onLogout }: { fornecedorId: string; onLogout: () => void }) {
  const route = useAppLocation();
  const active = MENU.some((item) => item.id === route.module) ? route.module : 'dashboard';
  const [snapshot, setSnapshot] = useState<SupplierSnapshot>(EMPTY);
  const [loading, setLoading] = useState(true);
  const [sidebar, setSidebar] = useState(false);
  const [productOpen, setProductOpen] = useState(false);
  const [deliveryOrder, setDeliveryOrder] = useState<SupplierOrder | null>(null);
  const [saving, setSaving] = useState(false);

  const load = useCallback(async () => {
    setLoading(true);
    try { setSnapshot(await getSupplierSnapshot()); }
    catch (error: any) { toast.error(error?.message || 'Não foi possível carregar o portal.'); }
    finally { setLoading(false); }
  }, []);

  useEffect(() => { void load(); }, [load]);

  const openOrder = async (order: SupplierOrder) => {
    setDeliveryOrder(order);
    if (order.status === 'enviado') {
      try { await markSupplierOrderSeen(order.id); await load(); } catch { /* atualização visual não bloqueia */ }
    }
  };

  const unread = snapshot.notifications.filter((item) => !item.lida).length;
  const pendingOrders = snapshot.orders.filter((item) => !['concluido', 'cancelado'].includes(item.status)).length;
  const pendingDeliveries = snapshot.deliveries.filter((item) => ['em_analise', 'ajuste_solicitado'].includes(item.status)).length;
  const receivable = snapshot.payables.filter((item) => item.status !== 'cancelado').reduce((sum, item) => sum + Number(item.valor_pendente || 0), 0);

  return <div className="min-h-screen bg-[#f5f5f2] text-neutral-900">
    <header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b border-neutral-200 bg-white/95 px-4 backdrop-blur lg:px-8"><div className="flex items-center gap-3"><button onClick={() => setSidebar(true)} className="rounded-xl p-2 lg:hidden"><Menu className="h-5 w-5" /></button><LogoGSA size="sm" variant="dark" /><div><p className="text-sm font-black">Portal do Fornecedor</p><p className="max-w-48 truncate text-[10px] font-semibold uppercase tracking-wider text-neutral-400">{snapshot.supplier?.nome_fantasia || snapshot.supplier?.razao_social || 'GSA Produtos'}</p></div></div><div className="flex items-center gap-2"><button onClick={() => void load()} className="rounded-xl border border-neutral-200 p-2.5"><RefreshCw className={`h-4 w-4 ${loading ? 'animate-spin' : ''}`} /></button><div className="relative rounded-xl border border-neutral-200 p-2.5"><Bell className="h-4 w-4" />{unread > 0 && <span className="absolute -right-1 -top-1 flex h-4 min-w-4 items-center justify-center rounded-full bg-red-500 px-1 text-[8px] font-black text-white">{unread}</span>}</div><button onClick={onLogout} className="rounded-xl bg-neutral-950 p-2.5 text-white" aria-label="Sair"><LogOut className="h-4 w-4" /></button></div></header>

    <div className="mx-auto flex max-w-[1500px]">
      <aside className="sticky top-16 hidden h-[calc(100vh-4rem)] w-64 shrink-0 border-r border-neutral-200 bg-white p-4 lg:block"><Navigation active={active} onNavigate={(path) => navigate(path)} /></aside>
      <main className="min-w-0 flex-1 p-4 lg:p-8">
        {active === 'dashboard' && <Dashboard snapshot={snapshot} pendingOrders={pendingOrders} pendingDeliveries={pendingDeliveries} receivable={receivable} onNavigate={(path) => navigate(path)} />}
        {active === 'produtos' && <Products snapshot={snapshot} loading={loading} onNew={() => setProductOpen(true)} />}
        {active === 'pedidos' && <Orders orders={snapshot.orders} loading={loading} onOpen={(order) => void openOrder(order)} />}
        {active === 'entregas' && <Deliveries deliveries={snapshot.deliveries} loading={loading} />}
        {active === 'financeiro' && <Payables payables={snapshot.payables} loading={loading} />}
      </main>
    </div>

    {sidebar && <div className="fixed inset-0 z-50 bg-black/50 lg:hidden" onClick={() => setSidebar(false)}><aside className="h-full w-72 bg-white p-4" onClick={(event) => event.stopPropagation()}><div className="mb-6 flex items-center justify-between"><p className="font-black">Menu</p><button onClick={() => setSidebar(false)}><X className="h-5 w-5" /></button></div><Navigation active={active} onNavigate={(path) => { setSidebar(false); navigate(path); }} /></aside></div>}

    <ProductRequestModal isOpen={productOpen} snapshot={snapshot} saving={saving} onClose={() => setProductOpen(false)} onSubmit={async (payload) => { setSaving(true); try { await requestSupplierProduct(payload); toast.success('Produto enviado para análise.'); setProductOpen(false); await load(); } catch (error: any) { toast.error(error?.message || 'Não foi possível enviar a solicitação.'); } finally { setSaving(false); } }} />
    <DeliveryModal order={deliveryOrder} supplierId={fornecedorId} saving={saving} onClose={() => setDeliveryOrder(null)} onSubmit={async (payload, xml, pdf) => { if (!deliveryOrder) return; setSaving(true); try { const [xmlReference, pdfReference] = await Promise.all([xml ? uploadSupplierInvoice(xml, fornecedorId, deliveryOrder.id) : Promise.resolve(null), pdf ? uploadSupplierInvoice(pdf, fornecedorId, deliveryOrder.id) : Promise.resolve(null)]); await submitSupplierDelivery(generateUUID(), deliveryOrder.id, { ...payload, arquivo_xml: xmlReference, arquivo_pdf: pdfReference }); toast.success('Entrega e nota fiscal enviadas para análise.'); setDeliveryOrder(null); await load(); } catch (error: any) { toast.error(error?.message || 'Não foi possível enviar a entrega.'); } finally { setSaving(false); } }} />
  </div>;
}

function Navigation({ active, onNavigate }: { active: string; onNavigate: (path: string) => void }) { return <nav className="space-y-1">{MENU.map((item) => { const Icon = item.icon; return <button key={item.id} onClick={() => onNavigate(item.path)} className={`flex w-full items-center gap-3 rounded-xl px-4 py-3 text-left text-sm font-bold ${active === item.id ? 'bg-neutral-950 text-white shadow-lg' : 'text-neutral-500 hover:bg-neutral-100 hover:text-neutral-900'}`}><Icon className="h-5 w-5" />{item.label}</button>; })}</nav>; }

function Dashboard({ snapshot, pendingOrders, pendingDeliveries, receivable, onNavigate }: { snapshot: SupplierSnapshot; pendingOrders: number; pendingDeliveries: number; receivable: number; onNavigate: (path: string) => void }) { return <div className="space-y-6"><section className="rounded-[2rem] bg-neutral-950 p-7 text-white"><p className="text-xs font-black uppercase tracking-[0.2em] text-emerald-300">Bem-vindo</p><h1 className="mt-2 text-3xl font-black">{snapshot.supplier?.nome_fantasia || snapshot.supplier?.razao_social || 'Fornecedor'}</h1><p className="mt-2 max-w-2xl text-sm text-white/60">Acompanhe produtos aprovados, pedidos de compra, entregas, notas fiscais e pagamentos.</p></section><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Produtos aprovados" value={snapshot.products.length} icon={Package} /><Metric label="Pedidos em aberto" value={pendingOrders} icon={ShoppingCart} /><Metric label="Entregas em análise" value={pendingDeliveries} icon={Truck} /><Metric label="Valores pendentes" value={formatCurrency(receivable)} icon={WalletCards} /></div><section className="rounded-2xl border border-neutral-200 bg-white p-6"><div className="flex items-center justify-between"><h2 className="font-black">Pedidos recentes</h2><button onClick={() => onNavigate(routes.supplier.orders())} className="text-xs font-black text-emerald-700">Ver todos</button></div><div className="mt-4 space-y-2">{snapshot.orders.slice(0, 5).map((order) => <div key={order.id} className="flex items-center justify-between rounded-xl bg-neutral-50 p-4"><div><p className="font-black">{order.codigo}</p><p className="text-xs text-neutral-500">{order.items?.length || 0} produto(s)</p></div><Status value={order.status} /></div>)}{snapshot.orders.length === 0 && <p className="py-8 text-center text-sm text-neutral-400">Nenhum pedido recebido.</p>}</div></section></div>; }

function Products({ snapshot, loading, onNew }: { snapshot: SupplierSnapshot; loading: boolean; onNew: () => void }) { return <Page title="Produtos fornecidos" description="Somente produtos aprovados podem ser incluídos em pedidos da GSA." action={<button onClick={onNew} className="flex items-center gap-2 rounded-xl bg-emerald-600 px-5 py-3 text-sm font-black text-white"><Plus className="h-4 w-4" />Solicitar produto</button>}><div className="grid gap-4 lg:grid-cols-2">{snapshot.products.map((product) => <article key={product.id} className="rounded-2xl border border-neutral-200 bg-white p-5"><div className="flex justify-between"><div><Status value={product.status} /><h2 className="mt-3 font-black">{product.produto_nome}</h2><p className="text-sm text-neutral-500">{product.codigo_produto || 'Sem código'} · custo {formatCurrency(Number(product.custo_unitario || 0))}</p></div><Package className="h-7 w-7 text-neutral-300" /></div><div className="mt-4 grid grid-cols-3 gap-2 text-center"><Small label="Mínimo" value={product.quantidade_minima} /><Small label="Prazo" value={`${product.prazo_entrega_dias} dias`} /><Small label="Estoque GSA" value={product.estoque_disponivel || 0} /></div></article>)}{!loading && snapshot.products.length === 0 && <Empty text="Nenhum produto aprovado ainda." />}</div><div className="mt-8"><h2 className="mb-3 font-black">Solicitações recentes</h2><div className="space-y-2">{snapshot.requests.map((request) => <div key={request.id} className="flex items-center justify-between rounded-xl border border-neutral-200 bg-white p-4"><div><p className="font-bold">{request.nome || snapshot.catalog.find((item) => item.id === request.produto_id)?.nome || 'Produto existente'}</p><p className="text-xs text-neutral-400">Enviado em {formatDateTime(request.created_at)}</p>{request.motivo_analise && <p className="mt-1 text-xs text-red-600">{request.motivo_analise}</p>}</div><Status value={request.status} /></div>)}</div></div></Page>; }

function Orders({ orders, loading, onOpen }: { orders: SupplierOrder[]; loading: boolean; onOpen: (order: SupplierOrder) => void }) { return <Page title="Pedidos de compra" description="Informe a entrega e a NF somente para os produtos e saldos deste pedido."><div className="space-y-3">{orders.map((order) => <button key={order.id} onClick={() => onOpen(order)} className="w-full rounded-2xl border border-neutral-200 bg-white p-5 text-left hover:border-emerald-300"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><Status value={order.status} /><h2 className="mt-2 text-lg font-black">{order.codigo}</h2><p className="text-sm text-neutral-500">{order.items?.length || 0} produto(s) · {formatCurrency(Number(order.valor_total_previsto || 0))}</p></div><div className="sm:text-right"><p className="text-[10px] font-black uppercase text-neutral-400">Previsão</p><p className="font-bold">{order.previsao_entrega ? formatDate(order.previsao_entrega) : 'Não informada'}</p></div></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{order.items?.map((item) => <div key={item.id} className="rounded-xl bg-neutral-50 p-3 text-sm"><p className="font-bold">{item.produto_nome_snapshot}</p><p className="text-xs text-neutral-500">Aprovado {item.quantidade_aprovada} de {item.quantidade_pedida}</p></div>)}</div></button>)}{!loading && orders.length === 0 && <Empty text="Nenhum pedido de compra recebido." />}</div></Page>; }

function Deliveries({ deliveries, loading }: { deliveries: Array<Record<string, any>>; loading: boolean }) { return <Page title="Entregas e notas fiscais" description="Acompanhe a análise administrativa de cada entrega."><div className="space-y-3">{deliveries.map((item) => <article key={item.id} className="rounded-2xl border border-neutral-200 bg-white p-5"><div className="flex justify-between gap-4"><div><Status value={item.status} /><h2 className="mt-2 font-black">NF {item.numero_nota}</h2><p className="text-sm text-neutral-500">{formatCurrency(Number(item.valor_total_nota || 0))} · emissão {formatDate(item.data_emissao)}</p>{item.motivo_analise && <p className="mt-2 rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-800">{item.motivo_analise}</p>}</div><FileCheck2 className="h-8 w-8 text-neutral-300" /></div></article>)}{!loading && deliveries.length === 0 && <Empty text="Nenhuma entrega enviada." />}</div></Page>; }

function Payables({ payables, loading }: { payables: Array<Record<string, any>>; loading: boolean }) { return <Page title="Financeiro" description="Contas criadas somente após a aprovação da entrega e da nota fiscal."><div className="space-y-3">{payables.map((item) => <article key={item.id} className="rounded-2xl border border-neutral-200 bg-white p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><Status value={item.status} /><h2 className="mt-2 font-black">{item.codigo} · NF {item.numero_documento}</h2><p className="text-sm text-neutral-500">Vencimento {formatDate(item.data_vencimento)}</p></div><p className="text-2xl font-black text-emerald-700">{formatCurrency(Number(item.valor_pendente || item.valor_original || 0))}</p></div></article>)}{!loading && payables.length === 0 && <Empty text="Nenhuma conta a pagar disponível." />}</div></Page>; }

function ProductRequestModal({ isOpen, snapshot, saving, onClose, onSubmit }: { isOpen: boolean; snapshot: SupplierSnapshot; saving: boolean; onClose: () => void; onSubmit: (payload: Record<string, unknown>) => Promise<void> }) { const [type, setType] = useState<'existente' | 'novo'>('existente'); const [form, setForm] = useState({ produto_id: '', nome: '', descricao: '', codigo_barras: '', custo_unitario: '', quantidade_minima: '1', prazo_entrega_dias: '0', observacoes: '' }); useEffect(() => { if (isOpen) setForm({ produto_id: snapshot.catalog[0]?.id || '', nome: '', descricao: '', codigo_barras: '', custo_unitario: '', quantidade_minima: '1', prazo_entrega_dias: '0', observacoes: '' }); }, [isOpen, snapshot.catalog]); return <Modal isOpen={isOpen} onClose={onClose} title="Solicitar produto" size="wide"><div className="space-y-5"><div className="grid grid-cols-2 gap-2 rounded-xl bg-neutral-100 p-1"><button onClick={() => setType('existente')} className={`rounded-lg py-3 text-sm font-black ${type === 'existente' ? 'bg-white shadow' : 'text-neutral-500'}`}>Produto existente</button><button onClick={() => setType('novo')} className={`rounded-lg py-3 text-sm font-black ${type === 'novo' ? 'bg-white shadow' : 'text-neutral-500'}`}>Novo produto</button></div>{type === 'existente' ? <label className="block text-sm font-bold">Produto da loja<select value={form.produto_id} onChange={(event) => setForm({ ...form, produto_id: event.target.value })} className="mt-2 w-full rounded-xl border border-neutral-200 p-3">{snapshot.catalog.map((item) => <option key={item.id} value={item.id}>{item.nome} · estoque {item.estoque_disponivel || 0}</option>)}</select></label> : <><Field label="Nome do produto" value={form.nome} onChange={(value) => setForm({ ...form, nome: value })} /><label className="block text-sm font-bold">Descrição<textarea rows={3} value={form.descricao} onChange={(event) => setForm({ ...form, descricao: event.target.value })} className="mt-2 w-full rounded-xl border border-neutral-200 p-3" /></label><Field label="Código de barras" value={form.codigo_barras} onChange={(value) => setForm({ ...form, codigo_barras: value.replace(/\D/g, '') })} /></>}<div className="grid gap-3 sm:grid-cols-3"><Field label="Custo unitário" type="number" value={form.custo_unitario} onChange={(value) => setForm({ ...form, custo_unitario: value })} /><Field label="Quantidade mínima" type="number" value={form.quantidade_minima} onChange={(value) => setForm({ ...form, quantidade_minima: value })} /><Field label="Prazo em dias" type="number" value={form.prazo_entrega_dias} onChange={(value) => setForm({ ...form, prazo_entrega_dias: value })} /></div><button disabled={saving || (type === 'existente' ? !form.produto_id : form.nome.trim().length < 3)} onClick={() => void onSubmit({ ...form, tipo: type, custo_unitario: Number(form.custo_unitario || 0), quantidade_minima: Number(form.quantidade_minima || 1), prazo_entrega_dias: Number(form.prazo_entrega_dias || 0) })} className="w-full rounded-xl bg-emerald-600 py-3 font-black text-white disabled:opacity-50">{saving ? 'Enviando...' : 'Enviar para análise'}</button></div></Modal>; }

function DeliveryModal({ order, supplierId, saving, onClose, onSubmit }: { order: SupplierOrder | null; supplierId: string; saving: boolean; onClose: () => void; onSubmit: (payload: Record<string, unknown>, xml: File | null, pdf: File | null) => Promise<void> }) { const [form, setForm] = useState({ numero_nota: '', serie_nota: '', chave_nfe: '', data_emissao: new Date().toISOString().slice(0, 10), valor_total_nota: '', vencimento: '', observacoes: '' }); const [items, setItems] = useState<Array<Record<string, any>>>([]); const [xml, setXml] = useState<File | null>(null); const [pdf, setPdf] = useState<File | null>(null); useEffect(() => { if (order) { setItems((order.items || []).filter((item) => item.quantidade_pedida > item.quantidade_aprovada).map((item) => ({ pedido_item_id: item.id, nome: item.produto_nome_snapshot, quantidade_entregue: String(item.quantidade_pedida - item.quantidade_aprovada), max: item.quantidade_pedida - item.quantidade_aprovada, custo_unitario_nota: String(item.custo_unitario), lote: '', validade: '' }))); setForm({ numero_nota: '', serie_nota: '', chave_nfe: '', data_emissao: new Date().toISOString().slice(0, 10), valor_total_nota: '', vencimento: order.vencimento_previsto || '', observacoes: '' }); setXml(null); setPdf(null); } }, [order]); return <Modal isOpen={!!order} onClose={onClose} title={`Informar entrega · ${order?.codigo || ''}`} size="wide">{order && <div className="space-y-5"><p className="rounded-xl bg-amber-50 p-3 text-xs font-semibold text-amber-800">O estoque e o pagamento só serão liberados após a conferência da GSA.</p><div className="grid gap-3 sm:grid-cols-3"><Field label="Número da NF" value={form.numero_nota} onChange={(value) => setForm({ ...form, numero_nota: value })} /><Field label="Série" value={form.serie_nota} onChange={(value) => setForm({ ...form, serie_nota: value })} /><Field label="Data de emissão" type="date" value={form.data_emissao} onChange={(value) => setForm({ ...form, data_emissao: value })} /></div><div className="grid gap-3 sm:grid-cols-2"><Field label="Chave NF-e (44 dígitos)" value={form.chave_nfe} onChange={(value) => setForm({ ...form, chave_nfe: value.replace(/\D/g, '').slice(0, 44) })} /><Field label="Valor total da nota" type="number" value={form.valor_total_nota} onChange={(value) => setForm({ ...form, valor_total_nota: value })} /></div><div><h3 className="mb-2 font-black">Quantidades entregues</h3><div className="space-y-2">{items.map((item, index) => <div key={item.pedido_item_id} className="grid gap-2 rounded-xl bg-neutral-50 p-3 sm:grid-cols-[1fr_110px_130px]"><div><p className="text-sm font-bold">{item.nome}</p><p className="text-xs text-neutral-400">Saldo do pedido: {item.max}</p></div><input type="number" min="0" max={item.max} value={item.quantidade_entregue} onChange={(event) => setItems((current) => current.map((entry, i) => i === index ? { ...entry, quantidade_entregue: event.target.value } : entry))} className="rounded-lg border border-neutral-200 p-2" /><input type="number" min="0" step="0.01" value={item.custo_unitario_nota} onChange={(event) => setItems((current) => current.map((entry, i) => i === index ? { ...entry, custo_unitario_nota: event.target.value } : entry))} className="rounded-lg border border-neutral-200 p-2" /></div>)}</div></div><div className="grid gap-3 sm:grid-cols-2"><label className="rounded-xl border border-dashed border-neutral-300 p-4 text-sm font-bold"><FileUp className="mb-2 h-5 w-5" />XML da nota<input type="file" accept=".xml,application/xml,text/xml" onChange={(event) => setXml(event.target.files?.[0] || null)} className="mt-2 block w-full text-xs" />{xml && <span className="mt-1 block text-emerald-700">{xml.name}</span>}</label><label className="rounded-xl border border-dashed border-neutral-300 p-4 text-sm font-bold"><FileUp className="mb-2 h-5 w-5" />PDF da nota<input type="file" accept=".pdf,application/pdf" onChange={(event) => setPdf(event.target.files?.[0] || null)} className="mt-2 block w-full text-xs" />{pdf && <span className="mt-1 block text-emerald-700">{pdf.name}</span>}</label></div><button disabled={saving || !form.numero_nota || Number(form.valor_total_nota) < 0 || (!xml && !pdf) || !items.some((item) => Number(item.quantidade_entregue) > 0)} onClick={() => void onSubmit({ ...form, chave_nfe: form.chave_nfe || null, valor_total_nota: Number(form.valor_total_nota), items: items.filter((item) => Number(item.quantidade_entregue) > 0).map((item) => ({ pedido_item_id: item.pedido_item_id, quantidade_entregue: Number(item.quantidade_entregue), custo_unitario_nota: Number(item.custo_unitario_nota) })) }, xml, pdf)} className="w-full rounded-xl bg-emerald-600 py-3 font-black text-white disabled:opacity-50">{saving ? 'Enviando...' : 'Enviar entrega e nota fiscal'}</button></div>}</Modal>; }

function Page({ title, description, action, children }: { title: string; description: string; action?: React.ReactNode; children: React.ReactNode }) { return <div className="space-y-6"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h1 className="text-2xl font-black">{title}</h1><p className="mt-1 text-sm text-neutral-500">{description}</p></div>{action}</header>{children}</div>; }
function Metric({ label, value, icon: Icon }: { label: string; value: string | number; icon: typeof Package }) { return <article className="rounded-2xl border border-neutral-200 bg-white p-5"><div className="flex justify-between"><div><p className="text-[10px] font-black uppercase tracking-wider text-neutral-400">{label}</p><p className="mt-2 text-2xl font-black">{value}</p></div><Icon className="h-6 w-6 text-emerald-600" /></div></article>; }
function Small({ label, value }: { label: string; value: string | number }) { return <div className="rounded-xl bg-neutral-50 p-3"><p className="text-[9px] font-black uppercase text-neutral-400">{label}</p><p className="mt-1 text-sm font-black">{value}</p></div>; }
function Status({ value }: { value: string }) { const good = ['ativo', 'aprovado', 'pago', 'concluido'].includes(value); const bad = ['reprovado', 'cancelado', 'suspenso'].includes(value); return <span className={`inline-flex rounded-full px-2.5 py-1 text-[9px] font-black uppercase ${good ? 'bg-emerald-50 text-emerald-700' : bad ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700'}`}>{String(value || '').replaceAll('_', ' ')}</span>; }
function Empty({ text }: { text: string }) { return <div className="col-span-full rounded-2xl border border-dashed border-neutral-200 p-12 text-center text-sm text-neutral-400">{text}</div>; }
function Field({ label, value, onChange, type = 'text' }: { label: string; value: string; onChange: (value: string) => void; type?: string }) { return <label className="block text-sm font-bold">{label}<input type={type} min={type === 'number' ? 0 : undefined} step={type === 'number' ? '0.01' : undefined} value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full rounded-xl border border-neutral-200 p-3" /></label>; }
