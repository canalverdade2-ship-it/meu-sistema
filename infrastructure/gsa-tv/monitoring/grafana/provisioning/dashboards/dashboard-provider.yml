# Grafana provisioning — provider de dashboards
apiVersion: 1

providers:
  - name: 'GSA TV'
    orgId: 1
    folder: 'GSA TV'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
